/node_modules
/.next
/out
/build
/data
*.log
# Env files — every variant, not just *.local, so secrets never leak
.env
.env.*
!.env.example
.DS_Store
next-env.d.ts
# Local tooling / editor config
/.claude
/.vscode
/.idea
*.pem
*.key
